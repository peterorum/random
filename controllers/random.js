(
    function()
    {
        "use strict";

        angular.module('randomApp').controller('RandomController', ['$scope', function($scope)
        {
            $scope.text = 'fish';
        }]);

    }()
);
